package
{
	import System.UI.Components.PriorityLoader;

	public dynamic class VisorLoader extends PriorityLoader
	{
		// Initialize
		//---------------------------------------------

		public function VisorLoader(menuName:String, mountID:String)
		{
			super(menuName, mountID);
		}

	}
}
